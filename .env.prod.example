POSTGRES_ROOT_PASSWORD=

# App database connection (assembled into DATABASE_URL in docker-compose.prod.yml)
PERA_DB_USER=
PERA_DB_PASSWORD=
PERA_DB_NAME=

# Allowed origin for HTTP + WebSocket CORS, e.g. https://app.example.com
CORS_ORIGIN=
